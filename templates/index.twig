{% extends "_layouts/_layout.twig" %}

{% set title = "Welcome" %}
{% set metaDescription = "Welcome to your new Craft CMS site" %}

{% block content %}
  {# Hero Section #}
  <section class="bg-gradient-to-r from-blue-600 to-blue-800 text-white">
    <div class="container-custom py-20">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-4xl md:text-6xl font-bold mb-6">
          🎉 Your Craft CMS Site is Ready!
        </h1>
        <p class="text-xl md:text-2xl mb-8 text-blue-100">
          This is a placeholder homepage. Start building your amazing website here.
        </p>
        <div class="bg-blue-700 bg-opacity-50 rounded-lg p-6 max-w-2xl mx-auto">
          <p class="text-lg text-blue-100 mb-4">
            <strong>Next steps:</strong> Visit the <a href="/admin" class="underline hover:text-white">admin panel</a> to create content, or edit this template at <code class="bg-blue-800 px-2 py-1 rounded">templates/index.twig</code>
          </p>
          <div class="bg-blue-800 bg-opacity-50 rounded-lg p-4 border border-blue-600">
            <p class="text-sm text-blue-200 mb-2"><strong>🔑 Default Admin Credentials:</strong></p>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm">
              <div>
                <span class="text-blue-300">Username:</span> 
                <code class="bg-blue-900 px-2 py-1 rounded text-white">admin</code>
              </div>
              <div>
                <span class="text-blue-300">Password:</span> 
                <code class="bg-blue-900 px-2 py-1 rounded text-white">password</code>
              </div>
            </div>
            <p class="text-xs text-blue-300 mt-2">⚠️ Change these credentials in production!</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  {# Tech Stack Info #}
  <section class="py-16 bg-gray-50">
    <div class="container-custom">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
            What's Included
          </h2>
          <p class="text-xl text-gray-600">
            Your development environment is ready with modern tools
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          <div class="card text-center">
            <div class="w-12 h-12 bg-orange-100 rounded-lg flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">⚡</span>
            </div>
            <h3 class="font-semibold mb-2">Craft CMS 5</h3>
            <p class="text-sm text-gray-600">Modern content management</p>
          </div>
          
          <div class="card text-center">
            <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">🔥</span>
            </div>
            <h3 class="font-semibold mb-2">Vite</h3>
            <p class="text-sm text-gray-600">Lightning-fast builds</p>
          </div>
          
          <div class="card text-center">
            <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">🎨</span>
            </div>
            <h3 class="font-semibold mb-2">TailwindCSS</h3>
            <p class="text-sm text-gray-600">Utility-first styling</p>
          </div>
          
          <div class="card text-center">
            <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl">⚡</span>
            </div>
            <h3 class="font-semibold mb-2">Alpine.js</h3>
            <p class="text-sm text-gray-600">Lightweight interactivity</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  {# Quick Demo #}
  <section class="py-16">
    <div class="container-custom">
      <div class="max-w-2xl mx-auto">
        <div class="text-center mb-8">
          <h2 class="text-3xl font-bold text-gray-900 mb-4">
            Everything Works!
          </h2>
          <p class="text-gray-600">
            Here's a simple demo to show Alpine.js and TailwindCSS are working
          </p>
        </div>
        
        {# Simple counter demo #}
        <div class="card text-center" x-data="{ count: 0 }">
          <h3 class="text-xl font-semibold mb-4">Interactive Counter</h3>
          <div class="text-4xl font-bold text-blue-600 mb-6" x-text="count"></div>
          <div class="flex justify-center space-x-4">
            <button @click="count--" class="btn btn-secondary">
              - Decrease
            </button>
            <button @click="count = 0" class="btn btn-secondary">
              Reset
            </button>
            <button @click="count++" class="btn btn-primary">
              Increase +
            </button>
          </div>
          <p class="text-sm text-gray-500 mt-4">
            This counter is powered by Alpine.js and styled with TailwindCSS
          </p>
        </div>
      </div>
    </div>
  </section>
{% endblock %}
